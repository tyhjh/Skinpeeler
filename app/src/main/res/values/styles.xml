<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="textColor">@color/theme_txt</item>
        <item name="bgColor">@color/themebg</item>
        <item name="itemBgColor">@color/theme_main</item>
        <item name="showImg">@drawable/ic_bg</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme2" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black_light</item>
        <item name="colorPrimaryDark">@color/text_gray</item>
        <item name="colorAccent">@color/black</item>
        <item name="textColor">@color/white</item>
        <item name="bgColor">@color/dimgrey</item>
        <item name="itemBgColor">@color/gray</item>
        <item name="showImg">@mipmap/pic_test</item>

    </style>

</resources>
